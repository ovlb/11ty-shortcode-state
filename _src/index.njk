<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shortcode Data Test</title>
  </head>
  <body>
    <main>

      {% for item in data %}
        <h2>{{ item.content }}</h2>
        {% addComponentCss item %}
      {% endfor %}
      {% for item in data %}
        <h2>{{ item.content }}</h2>
        {% addComponentCss item %}
      {% endfor %}
    </main>
  </body>
</html>